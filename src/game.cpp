#include "game.h"
#include <iostream>

Game::Game() {
    // Constructor is now simpler.
    // The main function handles database initialization.
}

void Game::run() {
    // The main game loop will go here in the future.
    std::cout << "Game is running." << std::endl;
}